p = t('.description')
p = t('.counter', total: @subject.ratings.count, finnished: @subject.ratings.count -  @subject.unfinnished_ratings.count, unfinnished: @subject.unfinnished_ratings.count)

.image
  = image_tag(@rating.emoji.image_name)
.distractors
  strong = t('.demand')
  = link_to @rating.stimuli.first.verbal, rating_path(@rating, { 'rating[choosen_id]' => @rating.stimuli.first}), class: 'button first', method: :put
  = link_to @rating.stimuli.last.verbal, rating_path(@rating, { 'rating[choosen_id]' => @rating.stimuli.last}), class: 'button second', method: :put